{
	"name": "remote-mcp-logto",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build:widget": "cd web && npm install && npm run build && cd .. && node scripts/inline-react-widget.js",
		"deploy": "npm run build:widget && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "1.19.1",
		"agents": "0.0.84",
		"zod": "^3.25.76"
	},
	"devDependencies": {
		"@types/node": "^22.15.18",
		"typescript": "5.9.3",
		"wrangler": "^4.42.0"
	}
}
